import './bootstrap';
import Alpine from 'alpinejs';
import 'flowbite';
import MultipartRelated from 'multipart-related'

window.Alpine = Alpine;
Alpine.start();

window.MultipartRelatedParser = MultipartRelated;
